{
  "name": "memecoin-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "start": "cd app && npm start",
    "test": "cd app && npm test"
  }
}
